{
  "name": "Network Interceptor (Refresh, POST Body) - MV3",
  "version": "1.0",
  "manifest_version": 3,
  "description": "Intercept requests with a refresh button, show POST body in table, use radio filters.",
  "permissions": ["webRequest", "tabs", "storage"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_popup": "popup.html"
  }
}
